# 中国大学サッカー版 FotMob — Master Development Specification v1.0

## 1. 文書情報

- 正式な基準文書: 本ファイル
- バージョン: 1.0
- 制定日: 2026-07-14
- 対象: Chugoku Football Dataのデータ設計、UI、同期、検証、公開運用
- 初期対象: JUFA中国 2026年度 中国大学サッカーリーグ1部
- 表示言語・タイムゾーン: 日本語、Asia/Tokyo
- 配信方式: GitHub Pages上の静的Webアプリ

本仕様と古い設計文書が矛盾する場合は、本仕様を優先する。機能追加時は本仕様の要件ID、データの取得根拠、受け入れテストを対応付ける。

## 2. 開発原則

1. 既存のFotMob風UI、ダークテーマ、モバイル優先設計を維持する。
2. 大会主催者、大学または正式提供者が公開する実在データだけを正本とする。
3. 公開値が存在しない項目は推測、補完、AI生成せず、`null`、空文字または「未掲載」とする。ただし、公開された生年月日から規則どおり算出する推定学年は、表示上「推定」と明示する場合に限り例外として認める。
4. 勝点、勝率、平均値、順位、出場時間など、公開記録から決定的に再現できる派生値は利用できる。計算規則と入力元を文書化する。
5. xG、xA、xT、レーティング、AI予測など、公開入力から再現できない期待値・評価値は実装しない。
6. `matches.json`、`players.json`、`teams.json`を正規化された正本とし、同じ事実を複数ファイルへ手入力しない。
7. 大きな派生集計はNode.jsで生成してJSONへ保存し、ブラウザで画面遷移ごとに再計算しない。
8. JSONは必要な画面で一度だけ読み込み、Promiseキャッシュを共有する。
9. すべての外部HTMLは文字列として解析し、画面には安全なDOM APIで描画する。
10. 新機能はデータ検証、レスポンシブ表示、Console Error 0件のPlaywrightテストを完了条件に含める。

### 2.1 推測禁止の適用

- 学年は掲載値を優先する。掲載がない場合は、公開された生年月日と日本の学年区分（4月2日〜翌4月1日）から推定できる。推定値は掲載値と区別し、JSONとUIで「推定」と明示する。標準的な1〜4年の範囲外は`null`とする。
- 平均先発年齢は、公開された生年月日と試合日から機械的に算出できるため派生値として許可する。ただし対象人数と欠損を無視したことを明示する。
- MVP、ベストイレブン、注目選手・試合は、AI推定ではなく、公開された選定基準による運営選定または主催者発表だけを利用する。
- 顔写真、会場写真、地図等は、主催者提供または利用許諾を確認できる素材だけを公開する。

## 3. 技術・アーキテクチャ

- HTML
- CSS
- JavaScript（ES Modules）
- Node.js（同期、生成、検証のみ）
- Cheerio（公開HTML解析）
- Playwright（HTTP Request Context、E2E）
- JSON（公開データベース）
- GitHub Actions / GitHub Pages
- localStorage（端末内ユーザー設定）

Next.js、FastAPI、PostgreSQL、実行時API、SSR、ユーザーアカウント用バックエンドは採用しない。通知は静的PWAで可能な端末内機能に限定し、サーバーPushが必要な通知は別途アーキテクチャ判断を行う。

## 4. データ可用性ルール

各項目は次のいずれかへ分類する。

| 区分 | 条件 | 表示・保存 |
| --- | --- | --- |
| 掲載値 | 主催者ページ・正式提供JSON等に明示 | 正規化して保存可能 |
| 決定的派生 | 掲載値だけから一意に計算可能 | 計算規則を固定して保存・表示可能 |
| 運営選定 | 基準、選定者、対象期間を公開 | 「運営選定」と明示して表示可能 |
| 未取得 | 公開元には存在するが同期できていない | 実装を待機し、値を捏造しない |
| 対象外 | 掲載値がなく、決定的に導けない | 保存・表示しない |

新しいJSONフィールドを追加する前に、フィールドごとに区分、情報源、欠損表現、更新方法を定義する。

## 5. 機能要件

凡例: `実装済み`、`一部実装`、`計画`、`提供データ待ち`、`対象外`。

### 5.1 試合（MATCH）

| ID | 要件 | 状態 |
| --- | --- | --- |
| MATCH-01 | 試合一覧、終了・未開催表示 | 実装済み |
| MATCH-02 | 得点経過、前後半スコア、試合時間 | 実装済み |
| MATCH-03 | スタメン、ベンチ、交代 | 実装済み |
| MATCH-04 | 警告、退場 | 実装済み |
| MATCH-05 | 審判、運営、観客数、会場、キックオフ | 実装済み |
| MATCH-06 | Home/Away、前期・後期、チーム複合フィルター | 実装済み |
| MATCH-07 | 中止・延期・中断を掲載状態どおり表示 | 実装済み |
| MATCH-08 | チーム名、会場、日付による試合検索 | 実装済み |
| MATCH-09 | 年度・1部・2部・ステージ別の日程／結果切替 | 実装済み（2025・2026年度） |

中止、延期、中断は一覧HTMLに状態が明示された場合だけ取り込む。日時変更を推測して状態判定しない。

### 5.2 順位表（STANDING）

| ID | 要件 | 状態 |
| --- | --- | --- |
| STANDING-01 | 通算・前期・後期順位 | 実装済み |
| STANDING-02 | 勝点、勝敗、得失点差 | 実装済み |
| STANDING-03 | ホーム順位、アウェイ順位 | 実装済み |
| STANDING-04 | 勝率 | 実装済み |
| STANDING-05 | 節ごとの順位と順位推移グラフ | 実装済み |
| STANDING-06 | 前節比の順位変動（↑↓→） | 実装済み |

順位決定規則は大会規定を優先し、現在の勝点、得失点差、総得点という規則と異なる場合は生成処理を更新する。

### 5.3 選手（PLAYER）

| ID | 要件 | 状態 |
| --- | --- | --- |
| PLAYER-01 | 一覧、詳細、検索 | 実装済み |
| PLAYER-02 | 背番号、ポジション、身長、体重、出身高校、出身地、所属大学 | 一部実装 |
| PLAYER-03 | 掲載学年または明示付き推定学年 | 実装済み（推定） |
| PLAYER-04 | 出場、先発、ベンチ入り、途中出場、途中交代、出場時間 | 実装済み |
| PLAYER-05 | ベンチ外数 | 計画 |
| PLAYER-06 | 得点、アシスト、G+A、イエロー、レッド | 実装済み |
| PLAYER-07 | 前期・後期出場時間 | 実装済み |
| PLAYER-08 | 試合別成績 | 実装済み |
| PLAYER-09 | 年度別成績 | 複数年度データ待ち |

ベンチ外は「そのチームの当該試合が存在し、選手が当該年度に有効登録され、先発・控えのどちらにも登録されなかった場合」と定義する。登録期間が不明な選手は誤集計を避けるため対象外とする。

### 5.4 チーム（TEAM）

| ID | 要件 | 状態 |
| --- | --- | --- |
| TEAM-01 | チームページ、写真、エンブレム、キット、カラー | 実装済み |
| TEAM-02 | 監督、ホームグラウンド、所属選手 | 実装済み（掲載欠損は未掲載） |
| TEAM-03 | 直近5試合、ホーム・アウェイ成績 | 実装済み |
| TEAM-04 | 平均得失点、クリーンシート、無得点試合 | 実装済み |
| TEAM-05 | チーム内ランキング | 実装済み |
| TEAM-06 | Head to Head | 実装済み（収録年度のみ） |
| TEAM-07 | 2チーム比較画面 | 計画 |

すべてのクラブカラー、画像、エンブレム、キット参照は`teams.json`で一元管理し、画面へURLや色を重複記述しない。

### 5.5 ランキング（RANKING）

| ID | 要件 | 状態 |
| --- | --- | --- |
| RANKING-01 | 得点、アシスト、G+A | 実装済み |
| RANKING-02 | 出場試合、出場時間 | 実装済み |
| RANKING-03 | イエロー、レッド | 実装済み |
| RANKING-04 | 先発、ベンチ入り、途中出場 | 実装済み |
| RANKING-05 | フル出場 | 実装済み |
| RANKING-06 | 学年別ランキング | 実装済み（推定学年を明示） |
| RANKING-07 | チーム別選手ランキング | 実装済み |
| RANKING-08 | チームスタッツランキング | 実装済み |

フル出場は所定の試合時間を満たし、途中交代・途中出場・退場がない出場として計算する。アディショナルタイムの扱いは大会記録形式に合わせて検証する。

### 5.6 分析（ANALYTICS）

| ID | 要件 | 状態 |
| --- | --- | --- |
| ANALYTICS-01 | 得点時間帯 | 計画 |
| ANALYTICS-02 | 失点時間帯 | 計画 |
| ANALYTICS-03 | シュート、被シュート、CK、FK、オフサイド、ファウル | 提供データ待ち |
| ANALYTICS-04 | セットプレー得点・失点 | 提供データ待ち |

時間帯区分は固定し、例として0–15、16–30、31–45+、46–60、61–75、76–90+を用いる。実装時にオウンゴールとアディショナルタイムの規則をテストする。

### 5.7 記録（RECORD）

| ID | 要件 | 状態 |
| --- | --- | --- |
| RECORD-01 | シーズン最多得点、最多アシスト、最多勝点 | 計画 |
| RECORD-02 | 最多観客、最多得点試合、最多失点試合 | 計画 |
| RECORD-03 | 最長連勝、最長無敗 | 計画 |
| RECORD-04 | 歴代順位、歴代得点王、歴代アシスト王 | 複数年度データ待ち |

同数記録は恣意的に1件へ絞らず、同順位または同記録としてすべて表示する。

### 5.8 特集（FEATURE）

| ID | 要件 | 状態 |
| --- | --- | --- |
| FEATURE-01 | ベストイレブン、月間・節間MVP | 運営基準待ち |
| FEATURE-02 | 節間ベストイレブン | 運営基準待ち |
| FEATURE-03 | 注目選手、注目試合 | 運営基準待ち |

主催者発表または運営選定データを専用JSONで管理し、試合・選手の集計値と混在させない。選定者、基準、対象期間、公開日を必須とする。

### 5.9 会場（VENUE）

| ID | 要件 | 状態 |
| --- | --- | --- |
| VENUE-01 | 会場ページ、開催試合一覧 | 計画 |
| VENUE-02 | 写真、地図、アクセス | 提供・許諾データ待ち |
| VENUE-03 | 会場検索 | 計画 |

### 5.10 審判（REFEREE）

| ID | 要件 | 状態 |
| --- | --- | --- |
| REFEREE-01 | 主審ページ、担当試合 | 計画 |
| REFEREE-02 | イエロー平均、レッド平均 | 計画 |

審判平均はその審判が主審として担当した試合だけを分母とする。同姓同名を安易に同一人物として統合しない。

### 5.11 ユーザー機能（USER）

| ID | 要件 | 状態 |
| --- | --- | --- |
| USER-01 | お気に入りチーム | 実装済み |
| USER-02 | お気に入り選手 | 計画 |
| USER-03 | 最近見た試合・選手 | 計画 |
| USER-04 | ホーム画面カスタマイズ | 計画 |
| USER-05 | 端末内通知、試合リマインダー | PWA実装後に計画 |

ユーザー設定はlocalStorageまたはIndexedDBへ保存し、個人を識別する情報を収集しない。通知権限はユーザー操作後にだけ要求する。

### 5.12 アプリ機能（APP）

| ID | 要件 | 状態 |
| --- | --- | --- |
| APP-01 | ダークモード | 実装済み（標準） |
| APP-02 | PWA、インストール | 計画 |
| APP-03 | 限定オフライン閲覧 | 計画 |
| APP-04 | データ更新通知 | 計画 |
| APP-05 | ウィジェット | 技術調査待ち |

### 5.13 検索（SEARCH）

| ID | 要件 | 状態 |
| --- | --- | --- |
| SEARCH-01 | 選手検索 | 実装済み |
| SEARCH-02 | チーム検索 | 計画 |
| SEARCH-03 | 試合検索 | 計画 |
| SEARCH-04 | 会場検索 | 会場データ待ち |

### 5.14 大学サッカー独自機能（UNIVERSITY）

| ID | 要件 | 状態 |
| --- | --- | --- |
| UNIVERSITY-01 | 卒業生アーカイブ、歴代所属選手、大学別OB | 複数年度・公開名簿待ち |
| UNIVERSITY-02 | 学年別成績 | 計画（推定学年の明示必須） |
| UNIVERSITY-03 | 年度別チーム成績 | 複数年度データ待ち |
| UNIVERSITY-04 | 新入生一覧、卒業予定選手一覧 | 計画（推定学年と登録期間の明示必須） |

### 5.15 シーズンダッシュボード（DASHBOARD）

| ID | 要件 | 状態 |
| --- | --- | --- |
| DASHBOARD-01 | 現在の節、首位チーム、得点王 | 一部実装 |
| DASHBOARD-02 | 次節の注目カード | 運営選定基準待ち |
| DASHBOARD-03 | 最新結果、次の試合 | 実装済み |

## 6. 明示的に実装しない機能

公開提供または決定的な算出根拠がない限り、次を保存・表示しない。

- 顔写真
- 利き足
- 市場価値
- xG、xA、xT
- レーティング
- AI評価・AI予測値
- ポゼッション
- パス成功率
- クロス成功率

これらが将来提供された場合でも、自動的に対象へ戻さず、本仕様の改訂とデータ利用確認を行う。

## 7. JSON設計

### 7.1 正本

- `site/data/seasons/{season}/matches.json`: 公開日程・試合記録
- `site/data/teams.json`: チームプロフィール、カラー、画像、キット
- `site/data/players.json`: 登録選手プロフィール

### 7.2 派生データ

- `site/data/seasons/{season}/team-stats.json`: 期間別順位、チーム成績、フォーム、順位推移、チームランキング
- `site/data/seasons/{season}/div2/team-stats.json`: 2部の期間別順位、ホーム／アウェイ順位、チーム成績、フォーム、順位推移、チームランキング
- `site/data/head-to-head.json`: 全年度・全リーグ・全ステージを横断した対戦成績。各試合に試合日、年度、リーグID・名称、ステージID・名称を保持する派生データ
- 将来の記録、会場、審判、検索索引も正本から再生成可能にする。

### 7.3 禁止事項

- 同じ選手プロフィールを試合JSONへ複製しない。
- エンブレム、キット、チームカラーのURL・値を画面コードへ直接埋め込まない。
- 不明値を0、架空名、仮画像で代用しない。
- 派生JSONを手作業だけで更新しない。

## 8. 非機能要件

| 分類 | 要件 |
| --- | --- |
| 性能 | JSONを必要時に一度だけ取得し、画像をLazy Loadする。LCP 2.5秒以下を目標とする |
| レスポンシブ | 320px以上のモバイルからデスクトップまで本文の横はみ出しを発生させない |
| アクセシビリティ | WCAG 2.2 AAを目標とし、キーボード、ラベル、色以外の状態表現を備える |
| セキュリティ | 外部HTMLを挿入せず、URLとホストを許可リストで検証する |
| データ品質 | schemaVersion、ID、参照、件数、合計値、期間合計を自動検証する |
| プライバシー | 公開根拠のある必要最小限の個人情報だけを扱う |
| 互換性 | 最新2世代を目安とする主要モダンブラウザへ対応する |
| 配信 | GitHub Pagesのプロジェクトサブパスと独自ドメインの双方で動作する |

## 9. 同期・公開フロー

```text
データ取得元
  → Playwright HTTP Request Context / Cheerio
  → 正規化された正本JSON
  → Node.js派生JSON生成
  → 構造・参照・競技整合性検証
  → Playwright E2E
  → Pull Request差分レビュー
  → GitHub Pages公開
```

- クリック操作によるスクレイピングは行わない。
- 件数急減、セレクタ不一致、詳細取得エラーがあれば正常JSONを上書きしない。
- 同期時に取得件数、変更件数、エラー数を報告する。
- 生HTML、Cookie、認証情報を`site/`へ保存しない。

## 10. 機能受け入れ条件

新機能は次をすべて満たして完了とする。

1. 表示項目の取得元または決定的な計算規則が文書化されている。
2. 欠損値で画面が壊れず、推測値やサンプル値を表示しない。
3. 正本と派生データ間に不要な重複がない。
4. History APIの戻る・進むと直接URLアクセスが動作する。
5. JSONはキャッシュされ、画面遷移ごとの再取得・再集計がない。
6. 主要モバイル幅とデスクトップ幅で表示を確認する。
7. データ検証が成功する。
8. Playwright E2Eが成功する。
9. Console Errorが0件である。
10. README、データモデル、ロードマップを必要に応じて更新する。

## 11. 仕様変更手順

- 新しいデータ項目または機能を追加するときは、本仕様の要件IDと状態を更新する。
- 取得不能な項目を推測で埋める仕様変更は認めない。
- 対象外機能を再検討する場合は、情報源、権利、欠損率、更新頻度、検証方法を先に提示する。
- 破壊的なJSON変更では`schemaVersion`を更新し、移行・後方互換方針を記録する。

## 12. 現在の重要な適合課題

1. ベンチ外を算出するには登録有効期間を確定する必要がある。
2. MVP等の特集は選定基準と運営データ形式を定めるまで実装しない。

## 13. 参照文書

- [README.md](README.md)
- [データ構造設計](docs/DATA_MODEL.md)
- [開発ロードマップ](docs/ROADMAP.md)
- [アーキテクチャ](docs/ARCHITECTURE.md)
- [使用技術](docs/TECH_STACK.md)
- [GitHub Actions設計](docs/GITHUB_ACTIONS.md)
